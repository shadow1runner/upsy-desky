packages:
  base: !include jarvis.yaml

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

web_server:
  auth:
    username: !secret web_user
    password: !secret web_password

ota:
  - platform: web_server
  - platform: esphome
    password: !secret ota_password
